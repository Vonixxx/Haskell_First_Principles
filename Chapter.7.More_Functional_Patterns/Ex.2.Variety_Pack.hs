------------------
-- Variety Pack --
------------------

-------
-- 1 --
-------------------------------
-- k (x, y) = x              --
-- k1 = k (3, True)          --
-- k2 = k ((4-1), 10)        --
-- k3 = k ("three", (1 + 2)) --
----------------------------------------------------
-- a) What is the type of k?                      --
-- b) What is the type of k1, k2 and k3?          --
-- c) Which will return the number 3 as a result? --
----------------------------------------------------

-- a) k :: (a,b) -> a

-- b) k1 :: Num a => (a,Bool) -> a
--    k2 :: (Num a,Num b) => (a,b) -> a
--    k3 :: Num b => (String,b) -> String

-- c) k3

-------
-- 2 --
-------------------------------------------------------
-- two :: (a, b, c) -> (d, e, f) -> ((a, d), (c, f)) --
-- two = ???                                         --
-------------------------------------------------------

two :: (a, b, c) -> (d, e, f) -> ((a, d), (c, f))
two (a,b,c) (d,e,f) = ((a,d),(c,f))
